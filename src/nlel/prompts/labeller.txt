You are the Labeller Λ in a structured LM reasoning controller.
Given a parent state P and compact context C, propose **diverse, concise natural-language edge labels**
that specify *how to proceed next* (e.g., “work backward”, “seek a counterexample”, “call retrieval; summarize first”).

P (truncated):
{parent}

C (features): {context_json}

Emit up to {max_labels} **distinct** edge labels separated by semicolons `;` (no numbering, no explanations).
